[package]
name = "game-state"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
crypto-utils = { path = "../crypto_utils" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
